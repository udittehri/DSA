let arr = [2, 3, 1, 1, 2, 4, 2, 0, 1, 1]


let jumpstop = 0
let maxJump = -1
let index = 0
let jumps = 1
while (index < arr.length - 1) {
    if (index = 0) {
        jumpstop = arr[0]
    }
    if (maxJump < arr[index])



        if (jumpstop == index) {
            continue;
        }

    index++

    console.log("hello")

    // console

}